🔹 1. Что такое Node (узел)

В связанном списке данные хранятся не в массиве, а в виде цепочки узлов.

Каждый узел (Node) состоит из двух частей:
	•	data → хранит значение (например, число).
	•	next → указатель на следующий узел в списке (или nullptr, если это последний элемент).

struct Node {
    int data;      // данные (значение)
    Node* next;    // указатель на следующий узел
    Node(int val) : data(val), next(nullptr) {} // конструктор
};
👉 Пример: создаём один узел
Node* node1 = new Node(10);  
Это узел со значением 10, и он пока ни с кем не связан (next = nullptr).

🔹 2. Как работает список

Если у нас несколько узлов, мы связываем их через next:
Node* node1 = new Node(10);
Node* node2 = new Node(20);
Node* node3 = new Node(30);

node1->next = node2; // node1 указывает на node2
node2->next = node3; // node2 указывает на node3

10 -> 20 -> 30