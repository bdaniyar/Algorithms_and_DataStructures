1. Что такое map и unordered_map?

    Оба — это ассоциативные контейнеры STL, которые хранят данные в формате ключ → значение (как словари в Python).

    map<int, string> students;
    students[1] = "Alice";
    students[2] = "Bob";

    Тут:
        •	1 и 2 — ключи (int).
        •	"Alice", "Bob" — значения (string).

🔹 2. map
	•	Хранит элементы в отсортированном порядке по ключу (обычно в виде бинарного дерева поиска — Red-Black Tree).
	•	Каждый ключ уникален.
	•	Вставка, удаление, поиск работают за O(log n).
	•	Подходит, если нужен порядок по ключу.

    #include <iostream>
    #include <map>
    using namespace std;

    int main() {
        map<int, string> m;
        m[2] = "Bob";
        m[1] = "Alice";
        m[3] = "Charlie";

        for (auto &p : m) {
            cout << p.first << " -> " << p.second << endl;
        }
    }

    1 -> Alice
    2 -> Bob
    3 -> Charlie
    ➡️ Заметь: элементы автоматически отсортированы по ключу.

🔹 3. unordered_map
	•	Хранит элементы в хэш-таблице (нет сортировки).
	•	Каждый ключ уникален.
	•	Вставка, удаление, поиск работают за O(1) в среднем.
	•	Подходит, если не нужен порядок, а нужна максимальная скорость.

Пример:
    #include <iostream>
    #include <unordered_map>
    using namespace std;

    int main() {
        unordered_map<int, string> um;
        um[2] = "Bob";
        um[1] = "Alice";
        um[3] = "Charlie";

        for (auto &p : um) {
            cout << p.first << " -> " << p.second << endl;
        }
    }
    3 -> Charlie
    1 -> Alice
    2 -> Bob
    ➡️ Порядок непредсказуемый (зависит от хэшей).

    🔹 5. Основные методы

    Одинаковые у обоих контейнеров:
        •	m[key] — доступ/создание элемента.
        •	m.at(key) — доступ (если нет → ошибка).
        •	m.insert({key, value}) — вставка.
        •	m.erase(key) — удалить по ключу.
        •	m.find(key) — найти элемент.
        •	m.size() — размер.
        •	m.empty() — проверка на пустоту.

        map<int, int> freq;
        freq[10] = 2;
        freq[5] = 4;

        if (freq.find(5) != freq.end()) {
            cout << "Есть ключ 5, значение = " << freq[5] << endl;
        }
    
    🔹 6. Когда использовать?
	•	Если тебе нужно отсортированное хранение (например, вывод в порядке) → бери map.
	•	Если тебе нужна максимальная скорость поиска/вставки, порядок не важен → бери unordered_map.


    Основные команды 

#include <iostream>
#include <map>
#include <unordered_map>
#include <vector>
using namespace std;

int main() {
    // --- СОЗДАНИЕ ---
    map<int, string> m;                 // отсортированное хранение
    unordered_map<int, string> um;      // быстрое хранение (без порядка)

    // --- ДОБАВЛЕНИЕ ---
    m[1] = "Alice";      // добавление (или замена значения)
    m[2] = "Bob";
    m.insert({3, "Charlie"});  // через insert

    um[1] = "Dog";
    um[2] = "Cat";
    um.insert({3, "Fox"});

    // --- ДОСТУП К ЭЛЕМЕНТАМ ---
    cout << "m[1] = " << m[1] << endl;     // Alice
    cout << "um[2] = " << um[2] << endl;   // Cat

    // --- ПРОВЕРКА НАЛИЧИЯ ---
    if (m.find(2) != m.end()) {
        cout << "Ключ 2 есть в map, значение: " << m[2] << endl;
    }

    if (um.count(5) == 0) {
        cout << "Ключа 5 нет в unordered_map" << endl;
    }

    // --- РАЗМЕР / ПУСТОТА ---
    cout << "Размер map: " << m.size() << endl;
    cout << "Пустой ли um? " << (um.empty() ? "Да" : "Нет") << endl;

    // --- УДАЛЕНИЕ ---
    m.erase(2);       // удалить по ключу
    um.erase(3);

    // --- ОБХОД ВСЕХ ЭЛЕМЕНТОВ ---
    cout << "\nСодержимое map (отсортировано):\n";
    for (auto &p : m) {
        cout << p.first << " -> " << p.second << endl;
    }

    cout << "\nСодержимое unordered_map (порядок случайный):\n";
    for (auto &p : um) {
        cout << p.first << " -> " << p.second << endl;
    }

    return 0;
}