#include <iostream>

using namespace std;
struct Node {
    int val;
    Node* next;
    Node(int x) : val(x), next(NULL){}
};

int main(){
    int n,x , pos, var;
    for(int i=0;i<n;i++){
        cin >> x;
        Node* node = new Node(x);
        
    }
}