🧩 Что значит “реверс linked list”?

Если у тебя есть список:
head → [1] → [2] → [3] → [4] → NULL

После реверса должно стать:

head → [4] → [3] → [2] → [1] → NULL

Для этого мы будем по очереди:
	•	хранить предыдущий элемент (prev),
	•	текущий (cur),
	•	и следующий (next),
и переворачивать связи между ними.
