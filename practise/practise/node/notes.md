🧩 Что такое Node

Node — это узел списка.
Каждый узел хранит:
	1.	значение (val)
	2.	указатель на следующий элемент (next)

[3] -> [7] -> [9] -> [11] -> NULL
где NULL значит «конец списка».

Констуктор
struct Node{
    int val;
    Node* next;
    Node(int x) : val(x), next(NULL){}
};

Node *head = NULL , *tail = NULL;



Отлично 🔥
Переходим на двусвязный список (Doubly Linked List) — это логичный следующий шаг после односвязного.
Разберём понятно и пошагово, как он устроен, чем отличается и как его писать.

А у двусвязного списка каждый узел знает и предыдущего, и следующего:
NULL ← [prev | val | next] ↔ [prev | val | next] ↔ [prev | val | next] → NULL
Это значит:
	•	можно двигаться вперёд и назад,
	•	легче удалять и вставлять элементы.

    struct Node {
    int val;
    Node* next;
    Node* prev;

    Node(int x): val(x), next(NULL), prev(NULL) {}
};

Node* head = NULL;
Node* tail = NULL;

int n;
cin >> n;
for (int i = 0; i < n; i++) {
    int x;
    cin >> x;
    Node* node = new Node(x);
    if (head == NULL) {
        head = tail = node;
    } else {
        tail->next = node;   // связываем с хвостом
        node->prev = tail;   // связываем назад
        tail = node;         // обновляем хвост
    }
}

🔁 4. Вывод списка вперёд и назад

Вперёд:
Node* cur = head;
while (cur != NULL) {
    cout << cur->val << " ";
    cur = cur->next;
}
Назад:
Node* cur = tail;
while (cur != NULL) {
    cout << cur->val << " ";
    cur = cur->prev;
}