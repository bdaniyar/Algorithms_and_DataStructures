🔹 Базовый синтаксис компаратора (через лямбду)
sort(vec.begin(), vec.end(),
     [](const T &a, const T &b) {
         return /* условие, при котором a должно быть перед b */;
     });

💡 Важно:
	•	функция должна возвращать true, если a должно идти раньше b,
и false — если нет.
	•	Если a и b равны, можно вернуть false.

🔹 1. Сортировка по возрастанию чисел
sort(v.begin(), v.end(),
     [](int a, int b) {
         return a < b;  // стандартное возрастание
     });

или просто без компаратора:
sort(v.begin(), v.end()); // то же самое

🔹 2. Сортировка по убыванию чисел

sort(v.begin(), v.end(),
     [](int a, int b) {
         return a > b;  // теперь большие идут раньше
     });
или проста:
sort(v.begin(), v.end(), greater<int>());  // из <functional>

🔹 3. Сортировка строк по длине короткие — раньше
sort(v.begin(), v.end(),
     [](const string &a, const string &b) {
         return a.size() < b.size();  // короткие строки раньше
     });

🔹 4. Сортировка строк по длине длинные — раньше

sort(v.begin(), v.end(),
     [](const string &a, const string &b) {
         return a.size() > b.size();  // длинные строки раньше
     });

🔹 5. Сортировка пар по второму элементу по возрастанию

sort(pairs.begin(), pairs.end(),
     [](const pair<int,int> &a, const pair<int,int> &b) {
         if (a.first == b.first)
             return a.second < b.second;
         return a.first < b.first;
     });

🔹 6. Сортировка пар по второму элементу по убыванию

sort(pairs.begin(), pairs.end(),
     [](const pair<int,int> &a, const pair<int,int> &b) {
         if (a.first == b.first)
             return a.second > b.second;
         return a.first > b.first;
     });

🔹 9. Сортировка в нечётно-чётном порядке

sort(v.begin(), v.end(),
     [](int a, int b) {
         if ((a % 2) == (b % 2))
             return a < b;     // оба нечётные или оба чётные — сортируем по возрастанию
         return (a % 2) == 1;  // нечётные должны идти первыми
     });

3️⃣ Сортировка по количеству цифр
sort(v.begin(), v.end(), [](int a, int b){
    string sa = to_string(a);
    string sb = to_string(b);
    if (sa.size() == sb.size()) return a < b;
    return sa.size() < sb.size();
});