🔹 Пример

Если ввести 30,
число 30 можно разложить так:
30 = 2 × 3 × 5
Значит, 3 разных простых числа → программа выведет 3.

⸻
long long n;
cin >> n;
int count = 0;
→ читаем число n,
→ создаём счётчик count, где будем считать, сколько нашли простых делителей.

for (long long i = 2; i * i <= n; i++)

→ начинаем проверять все числа i от 2 и выше,
но не до n, а только до квадратного корня из n —
потому что дальше уже не нужно (если число делится на что-то большее, то на меньшее оно делится раньше).
if (n % i == 0) {
    count++;

→ если n делится на i без остатка,
значит, i — один из простых делителей.
Добавляем 1 в count.

while (n % i == 0)
    n /= i;

→ теперь делим n на i, пока оно делится,
чтобы убрать все одинаковые множители.

Пример:
если n = 16,
то 16 / 2 = 8 → 8 / 2 = 4 → 4 / 2 = 2 → 2 / 2 = 1.
То есть мы полностью убрали все двойки.

После цикла:
if (n > 1)
    count++;

→ если после всех делений в n всё ещё осталось число больше 1,
это число само простое и его нужно посчитать.

(Например, если в конце осталось 5, значит 5 — ещё один простой делитель.)

⸻
cout << count << endl;

