vector<int> v;  // пустой вектор

📦 Характеристики:
	• Размер изначально = 0.
	• Память увеличивается автоматически при добавлении элементов.
	• Элементы добавляются через push_back().

⚙️ Основные функции:

Функция		Назначение		Пример
push_back(x)	добавить элемент в конец	v.push_back(10);
pop_back()	удалить последний элемент	v.pop_back();
size()		текущее количество элементов	v.size();
empty()		проверить, пуст ли вектор	if(v.empty())
clear()		удалить все элементы		v.clear();
back()		вернуть последний элемент	cout << v.back();
front()		вернуть первый элемент		cout << v.front();
[]		доступ по индексу		v[0] = 5;
at(i)		безопасный доступ (с проверкой)	v.at(0);
resize(n)	изменить размер			v.resize(10);
reserve(n)	зарезервировать память		v.reserve(100);

vector<int> v;
v.push_back(5);
v.push_back(8);
cout << v[1]; // 8

🔹 2. Вектор с фиксированным размером

Создание:
	vector<int> v(5); // размер = 5, все элементы = 0
	vector<int> v(5, -1); // размер = 5, все элементы = -1

📦 Характеристики:
	• Размер изначально фиксирован.
	• Можно обращаться по индексу (v[i]).
	• push_back() тоже работает, но расширяет вектор за пределы исходного размера.
	• Если нужен строго фиксированный размер (без изменения), то просто не вызывай push_back().

⚙️ Основные функции:

Функция		Назначение		Пример
v[i]		доступ по индексу		v[2] = 10;
size()		узнать размер			cout << v.size();
assign(n, val)	заполнить вектор n элементами	v.assign(5, 100);
fill(v.begin(), v.end(), val)	заполнить весь вектор	fill(v.begin(), v.end(), 0);

vector<int> v(3, 7);
for(int i = 0; i < 3; i++) cout << v[i] << " "; // 7 7 7

🔹 Сравнение

Свойство			Без размера			С фиксированным
Создание			vector<int> v;			vector<int> v(n);
Память				увеличивается динамически	выделяется сразу
Добавление			через push_back()		можно писать v[i] = ...
Безопасность			возможен out_of_range при v[i]	безопаснее при at()
Использование			удобно, если размер заранее неизвестен	удобно, если размер заранее известен
Скорость			немного медленнее при росте (из-за realocation)	стабильная скорость, память уже выделена
